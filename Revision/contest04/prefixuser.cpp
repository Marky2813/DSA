#include<iostream>
#include<set>
#include<vector>
using namespace std;
 
int main() {
    int n;
    cin >> n;  
    vector<long long> user(n); 
    for(int i = 0; i < n; i++) {
        cin >> user[i]; 
    }
    set<long long> st; 
    for(int i = 0; i < n; i++) 
}